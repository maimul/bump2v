[build-system]
requires = ["setuptools", "setuptools_scm", "wheel", "bump2version"]
build-backend = "setuptools.build_meta"

[project]
name = "bump2v"
version = "0.0.6"

[project.scripts]
bumptydumpty = "bump2v.__main__:main"